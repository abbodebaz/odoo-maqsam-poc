<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_wati_universal_timeline_wizard_form" model="ir.ui.view">
        <field name="name">wati.universal.timeline.wizard.form</field>
        <field name="model">wati.universal.timeline.wizard</field>
        <field name="arch" type="xml">
            <form string="سجل WhatsApp">
                <sheet>
                    <div class="oe_title">
                        <h2>سجل WhatsApp</h2>
                        <div class="text-muted">يعرض تاريخ WhatsApp المرتبط بالسجل الحالي من خلال العميل أو رقم الجوال.</div>
                    </div>
                    <group>
                        <group>
                            <field name="record_name"/>
                            <field name="partner_id"/>
                        </group>
                        <group>
                            <field name="phone"/>
                            <field name="message_count"/>
                            <field name="last_message_at"/>
                        </group>
                    </group>
                    <field name="rule_id" invisible="1"/>
                    <field name="source_model" invisible="1"/>
                    <field name="source_res_id" invisible="1"/>
                    <field name="message_ids" readonly="1" nolabel="1">
                        <list create="false" edit="false" delete="false" default_order="received_at desc, id desc"
                              decoration-success="direction == 'outbound'"
                              decoration-info="direction == 'inbound'">
                            <field name="received_at" string="الوقت"/>
                            <field name="direction" string="الاتجاه" widget="badge"/>
                            <field name="message_type" string="النوع" optional="hide"/>
                            <field name="text" string="الرسالة"/>
                            <field name="status" string="الحالة" widget="badge"/>
                            <field name="operator_name" string="الموظف" optional="show"/>
                            <field name="channel_phone_number" string="القناة" optional="hide"/>
                        </list>
                    </field>
                </sheet>
                <footer>
                    <button name="action_open_full_timeline" type="object" string="فتح السجل الكامل" class="btn-primary" icon="fa-external-link"/>
                    <button string="إغلاق" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_wati_universal_timeline" model="ir.actions.act_window">
        <field name="name">سجل WhatsApp</field>
        <field name="res_model">wati.universal.timeline.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
